# AI Worker YOLO confidence threshold.
# Higher = fewer false positives, lower = more sensitive.
AI_WORKER_CONF=0.5

# RTSP transport fallback order. default = let FFmpeg/camera negotiate.
AI_WORKER_RTSP_TRANSPORTS=default,udp,tcp
AI_WORKER_RTSP_BUFFER_SIZE=1
